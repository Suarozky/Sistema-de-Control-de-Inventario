<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Inventario" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="turbo-visit-control" content="reload">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <!-- ✅ CSS -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <!-- ✅ JS (sin extras, Importmap ya gestiona Turbo/Stimulus) -->
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 flex min-h-screen">
    <!-- Sidebar (solo si no está en rutas restringidas) -->
    <% unless [
        root_path, 
        new_user_path, 
        new_product_path
      ].include?(request.path) || 
         request.path.match?(/^\/users\/\d+\/my_products$/) || 
         request.path.match?(/^\/products\/\d+\/transactions_history$/) %>
      
      <%= render "shared/sidebar" %>
    <% end %>

    <!-- Contenido principal -->
    <div class="flex-1 flex flex-col">
      <% if content_for?(:header) %>
        <header class="bg-white shadow p-4">
          <%= yield :header %>
        </header>
      <% end %>

      <main class="flex-1 w-full p-5">
        <% flash.each do |type, message| %>
          <div class="rounded-md <%= flash_class(type) %>">
            <%= message %>
          </div>
        <% end %>
        <%= yield %>
      </main>

      <% if content_for?(:footer) %>
        <footer class="mt-auto p-4 bg-white border-t">
          <%= yield :footer %>
        </footer>
      <% end %>
    </div>
  </body>
</html>
