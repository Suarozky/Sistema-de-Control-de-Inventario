<div class="flex items-center justify-between p-4 rounded-lg border border-blue-200 bg-blue-50 shadow-sm mb-3">
  <!-- Izquierda -->
  <div class="flex items-start gap-3">
    <!-- Icono -->
    <div class="w-8 h-8 flex items-center justify-center rounded-full bg-blue-100 text-blue-600">
      <i class="fas fa-shopping-bag"></i>
    </div>

    <!-- Texto -->
    <div>
      <h3 class="font-semibold text-gray-800">Nueva transacción</h3>
      <p class="text-sm text-gray-600">
        <%= transaction.product&.model %> - <%= transaction.owner&.name %> <%= transaction.owner&.lastname %>
      </p>
    </div>
  </div>

  <!-- Derecha -->
  <div class="text-right">
   
    <p class="text-xs text-gray-500">
      Hace <%= time_ago_in_words(transaction.date) %>
    </p>
  </div>
</div>
